import React from 'react';
import ReactDOM from 'react-dom/client';
import LocalBattleDemo from './LocalBattleDemo';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LocalBattleDemo />
  </React.StrictMode>
);

